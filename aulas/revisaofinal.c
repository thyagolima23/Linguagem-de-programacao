//iniciar as bibliotecas:
#include <stdio.h>
#include <stdlib.h>

//metodo de abertura
main(){
        //tipos de dados:
        int numeros_inteiros;
        char strings;
        float numeros_decimais;
        double numeros_decimais_grandes;
        char strings_maiores [50];
        int valor[5];
        //tipo multimensional
        int matriz[5][5];
        Calculo_salario(matriz[5][5]);
        //if = se
        if(salario > 5000.00){//se verdadeiro faça:
                desconto_imposto();
        }
        printf();//mostrar uma mensagem para o usuario
        scanf();// receber valores via teclado
        //laços
        while(i<10){}//Enquanto a condição for verdadeira faça.
        //enquanto i for menor que 10, os comandos dentro das chaves serão executados
        for(int i=0; i<10; i++){}
        //para i = 0 até i < 10 faça. O i++ serve para incrementar a variavel.
        switch (){
        case:
                break;
        default:
                break;
        }
}
float Calculo_salario(int matriz[5][5]){ 

}
float desconta_imposto(){ 
}